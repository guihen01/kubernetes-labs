apiVersion: v1
kind: Service
metadata:
  name: demo-app
  labels:
    app: demo-app
spec:
  selector:
    app: demo-app
  ports:
    - name: http   # ⚠️ IMPORTANT
      port: 80
      targetPort: 80
